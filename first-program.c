#include <stdio.h>

int main() {
    char message[] = "hello world"; // declaring and initializing the variable message
    printf("%s\n", message); // using format specifier %s instead of hard-coding the message 
    return 0;
}